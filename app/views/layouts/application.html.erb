<!DOCTYPE html>
<html>
  <head>
    <title>AjaxApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.0/jquery.min.js"></script>

  </head>

  <body>
    <header class="bg-warning">
      <div class="container">
        <div class="row">
          <div class="col-3">
            <%= link_to root_path do %>
              <h3 class="pl-auto text-dark">Bookers</h3>
            <% end %>
          </div>
          <div class="col-5">
            <% if user_signed_in? %>
              <li>
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-light" %>
              </li>
              <li>
                <%= link_to "本一覧", books_path, class: "btn btn-light" %>
              </li>
            <% else %>
              <li>
                <%= link_to "新規登録", new_user_registration_path, class: "btn btn-light" %>
              </li>
              <li>
                <%= link_to "ログイン", new_user_session_path, class: "btn btn-light" %>
              </li>
            <% end %>
          </div>
        </div>
      </div>
    </header>

    <main class="mt-5">
      <%= yield %>
    </main>
  </body>
</html>
